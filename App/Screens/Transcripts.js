import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Transcripts = () => {
  return (
    <View style={{backgroundColor: 'white', flex: 1}}>
      <Text>Transcripts Dashboard</Text>
    </View>
  );
};

export default Transcripts;

const styles = StyleSheet.create({});
